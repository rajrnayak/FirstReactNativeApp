import { Text, View } from "react-native";

export default function Form() {
	return (
		<></>
		// <Modal
		// 	animationType="slide"
		// 	transparent={true}
		// 	visible={modalVisible}
		// 	onRequestClose={() => {
		// 		setModalVisible(!modalVisible);
		// 	}}>
		// 	<View style={styles.centeredView}>
		// 		<View style={styles.modalView}>
		// 			<View></View>
		// 			<Pressable style={[styles.button, { right: 90 }]} onPress={() => setModalVisible(!modalVisible)}>
		// 				<Text style={styles.textStyle}>Submit</Text>
		// 			</Pressable>
		// 			<Pressable style={[styles.button, { backgroundColor: "lightgrey" }]} onPress={() => setModalVisible(!modalVisible)}>
		// 				<Text style={[styles.textStyle, { color: "black" }]}>Cancel</Text>
		// 			</Pressable>
		// 		</View>
		// 	</View>
		// </Modal>
	);
}
